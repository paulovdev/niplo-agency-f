"use client";

export const PerspectiveMenu = ({ label }) => {
  return (
    <div className="size-full bg-none">
      <div className="font-general text-color text-[.9rem] tracking-[-.5px] font-[600] uppercase ">
        {label}
      </div>
    </div>
  );
};
