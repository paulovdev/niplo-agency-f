"use client";

export const PerspectiveMenu = ({ label }) => {
  return (
    <div className="size-full bg-none">
      <div className="font-general text-color3 text-[.7rem] tracking-[-.5px] font-[500] uppercase ">
        {label}
      </div>
    </div>
  );
};
